# Phase 999.5: Excel-to-System Pipeline — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-30
**Phase:** 999.5-excel-to-system-pipeline-backlog
**Areas discussed:** Import entry point, Confirmation UX, Re-import & reconciliation, Seed data pipeline, LLM seed data (cross-phase)

---

## Import Entry Point

| Option | Description | Selected |
|--------|-------------|----------|
| CLI only (feather import) | Terminal-based, consistent with 999.3 | |
| Web upload + CLI | Primary: drag-and-drop web route. Also CLI for power users | ✓ |
| Claude Code skill | Like /feather:architect — runs in terminal | |

**User's choice:** Web upload + CLI
**Notes:** None

### Schema Mapping Persistence

| Option | Description | Selected |
|--------|-------------|----------|
| Convex table | Store in DB, web-accessible | |
| Local JSON files | Store in .feather/mappings/, version-controllable | |
| Both (Convex primary, JSON export) | Convex source of truth + feather export-mappings | ✓ |

**User's choice:** Both (Convex primary, JSON export)

### Web Location

| Option | Description | Selected |
|--------|-------------|----------|
| Standalone /import route | Full-page dedicated route | ✓ |
| Dashboard widget | Embedded in dashboard | |
| You decide | Claude picks | |

**User's choice:** Standalone /import route

---

## Confirmation UX

### Layout Approach

| Option | Description | Selected |
|--------|-------------|----------|
| Step-by-step wizard | 3-step: entities → fields → relationships | ✓ |
| Single-page dashboard | All entities/fields visible at once, tabbed | |
| Interactive table | Flat spreadsheet view of all columns | |

**User's choice:** Step-by-step wizard
**Notes:** User requested visual mockups of all three options before deciding. ASCII previews were shown.

### Correction Mechanism

| Option | Description | Selected |
|--------|-------------|----------|
| Inline dropdown | Click type cell → dropdown | ✓ |
| Edit modal | Click row → modal with all field properties | |
| You decide | Claude picks | |

**User's choice:** Inline dropdown

### Sample Data

| Option | Description | Selected |
|--------|-------------|----------|
| Always show sample rows | 2-3 rows from Excel alongside fields | ✓ |
| Show on demand | Collapsed by default | |
| No sample data in wizard | Only after YAML generation | |

**User's choice:** Always show sample rows

### Post-Wizard Review

| Option | Description | Selected |
|--------|-------------|----------|
| Dashboard summary | Single-page with all entities, inline editing | ✓ |
| Quick summary only | Compact text, Back to re-enter wizard | |

**User's choice:** Dashboard summary (Option B from initial choices)
**Notes:** User identified that wizard is great for guided first pass, but dashboard needed for full-picture review before confirming.

### Large Excel Handling (30+ columns)

| Option | Description | Selected |
|--------|-------------|----------|
| Smart grouping by LLM | Semantic groups, low-confidence expanded first | ✓ |
| Confidence-based triage | Only show low-confidence fields | |
| Paginated (10 per page) | Simple pagination | |

**User's choice:** Smart grouping by LLM

### Sample Data for Large Excel

**User's choice:** Both views togglable — default: inline values per field, toggle: full sample rows per group
**Notes:** User explicitly requested both views be available as a toggle.

---

## Re-import & Reconciliation

### Re-import Detection

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-detect from content | Compare fingerprints against existing mappings | |
| Explicit flag | User chooses "Update existing import" | |
| Both (auto-detect + manual override) | Auto-detect default, user can override | ✓ |

**User's choice:** Both (auto-detect + manual override)

### Diff UI

**User's choice:** Both views togglable — default: flat change list with action dropdowns, toggle: git-style side-by-side diff
**Notes:** User explicitly requested both views be togglable, consistent with sample data toggle pattern.

### Removed Column Default Action

| Option | Description | Selected |
|--------|-------------|----------|
| Archive (hide, keep data) | Safest default, no data loss | |
| Prompt every time | Always ask for each removed column | |

**User's choice:** Prompt every time, with "Archive all & review later" escape hatch for large imports
**Notes:** User referenced Gmail contact import as UX inspiration — import history should provide a link to go back and review decisions. Each import event should be an auditable, revisitable permalink (`/imports/{id}`).

---

## Seed Data Pipeline

### Data Import Trigger

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-import after confirm | One flow: confirm → YAML → code → data | ✓ |
| Separate "Import data" step | Generate code first, import later | |
| You decide | Claude picks | |

**User's choice:** Auto-import after confirm

### Error Handling

| Option | Description | Selected |
|--------|-------------|----------|
| Skip & report | Import valid, skip bad, show summary | |
| Best-effort coercion | Try to fix types/defaults | |
| Both (coerce + report) | Auto-coerce safe, skip unfixable, report all | ✓ (modified) |

**User's choice:** Three-tier with user review — auto-fix safe coercions (green), propose fixes for ambiguous (yellow, user confirms), skip unfixable (red). Always show defaults per Tableau Standard, let user override.
**Notes:** User emphasized wanting to see issues and have input on resolution, with smart defaults always presented.

---

## LLM Seed Data (Cross-Phase Discussion)

### Seed Data Realism

| Option | Description | Selected |
|--------|-------------|----------|
| Domain-realistic (smart) | LLM uses domain context for realistic data | ✓ |
| Minimal placeholders | Generic User 1, User 2 | |
| User-guided | User provides examples, LLM extrapolates | |

**User's choice:** Domain-realistic (smart)

### Scope Assignment

| Option | Description | Selected |
|--------|-------------|----------|
| Phase 999.3 (Architect Conversation) | Seed data as end-of-conversation deliverable | ✓ |
| Phase 999.5 (this phase) | Secondary input path alongside Excel | |
| Shared utility | Both phases use same generator | |

**User's choice:** Phase 999.3
**Notes:** Import pipeline (dependency ordering, error handling, import history) is reusable across both phases.

---

## Claude's Discretion

- Web framework choices for wizard/dashboard UI
- Confidence thresholds (re-import detection, column grouping)
- Import report page layout
- CLI argument parsing for `feather import`

## Deferred Ideas

- LLM-generated seed data → Phase 999.3 scope
- Flat data → entity splitting UX
- File format expansion (CSV, Google Sheets, Airtable)
- Performance / large file handling (100MB+, 50k+ rows)
