---
phase: 01-architecture-modernization
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/features/dashboard/components/DashboardPage.tsx
  - src/features/dashboard/components/Navigation.tsx
  - src/features/dashboard/hooks/.gitkeep
  - src/features/dashboard/index.ts
  - src/features/dashboard/dashboard.test.tsx
  - src/features/billing/components/CheckoutPage.tsx
  - src/features/billing/components/BillingSettings.tsx
  - src/features/billing/hooks/.gitkeep
  - src/features/billing/index.ts
  - src/features/billing/billing.test.tsx
  - src/features/settings/components/SettingsPage.tsx
  - src/features/settings/hooks/.gitkeep
  - src/features/settings/index.ts
  - src/features/settings/settings.test.tsx
autonomous: true
requirements:
  - STRUCT-01
  - STRUCT-04

must_haves:
  truths:
    - "Dashboard, billing, and settings UI logic lives in src/features/ with barrel exports"
    - "Each extracted feature has components/, hooks/, and index.ts"
    - "Tests for dashboard, billing, settings are co-located in feature folders"
    - "TypeScript compiles after extraction"
  artifacts:
    - path: "src/features/dashboard/index.ts"
      provides: "Dashboard feature barrel export"
    - path: "src/features/settings/index.ts"
      provides: "Settings feature barrel export"
    - path: "src/features/billing/index.ts"
      provides: "Billing feature barrel export"
    - path: "src/features/dashboard/components/DashboardPage.tsx"
      provides: "Extracted dashboard page component"
      min_lines: 20
    - path: "src/features/settings/components/SettingsPage.tsx"
      provides: "Extracted settings page component"
      min_lines: 20
  key_links:
    - from: "src/features/dashboard/index.ts"
      to: "src/features/dashboard/components/DashboardPage.tsx"
      via: "barrel export"
      pattern: "from.*DashboardPage"
    - from: "src/features/billing/index.ts"
      to: "src/features/billing/components/CheckoutPage.tsx"
      via: "barrel export"
      pattern: "from.*CheckoutPage"
---

<objective>
Extract dashboard, billing, and settings frontend UI logic from fat route files into src/features/ domain folders. This is the first half of the frontend feature-folder extraction -- the three most substantial features.

Purpose: Move the heaviest UI code (dashboard, settings ~232 lines, billing checkout) into proper feature folders with components, hooks, and barrel exports.
Output: 3 feature folders with extracted components and co-located tests.
</objective>

<execution_context>
@/Users/siraj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/siraj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-architecture-modernization/01-RESEARCH.md
@.planning/phases/01-architecture-modernization/01-02-SUMMARY.md

@src/routes/_app/_auth/dashboard/_layout.index.tsx
@src/routes/_app/_auth/dashboard/_layout.checkout.tsx
@src/routes/_app/_auth/dashboard/_layout.settings.index.tsx
@src/routes/_app/_auth/dashboard/_layout.settings.billing.tsx
@src/routes/_app/_auth/dashboard/-ui.navigation.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract dashboard and navigation into feature folder</name>
  <files>src/features/dashboard/components/DashboardPage.tsx, src/features/dashboard/components/Navigation.tsx, src/features/dashboard/hooks/.gitkeep, src/features/dashboard/index.ts, src/features/dashboard/dashboard.test.tsx</files>
  <action>
Create the dashboard feature directory structure:
```
src/features/dashboard/
  components/
    DashboardPage.tsx
    Navigation.tsx
  hooks/
    .gitkeep
  index.ts
  dashboard.test.tsx
```

1. Extract the component from `_layout.index.tsx` into `src/features/dashboard/components/DashboardPage.tsx`. Move ALL JSX, hooks, helper functions from the route file. The component should be a named export: `export function DashboardPage()`.

2. Extract navigation from `-ui.navigation.tsx` into `src/features/dashboard/components/Navigation.tsx`. Dashboard shell (nav + header layout) lives here per CONTEXT.md (Claude's discretion).

3. Update imports to use the new api.* paths from Plan 01-02 (e.g., `api.users.queries.getCurrentUser`).

4. Create barrel export `index.ts`:
```typescript
export { DashboardPage } from "./components/DashboardPage";
export { Navigation } from "./components/Navigation";
```

5. Move dashboard-related tests to `src/features/dashboard/dashboard.test.tsx`. Update test imports to reference the new component locations.

IMPORTANT: Do NOT modify route files yet -- that happens in Plan 01-04. Only extract components.
  </action>
  <verify>
    <automated>cd /Users/siraj/Desktop/NonDropBoxProjects/feather-starter-convex && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>Dashboard feature folder exists with DashboardPage, Navigation components, barrel export, and co-located test. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Extract billing and settings into feature folders</name>
  <files>src/features/billing/components/CheckoutPage.tsx, src/features/billing/components/BillingSettings.tsx, src/features/billing/hooks/.gitkeep, src/features/billing/index.ts, src/features/billing/billing.test.tsx, src/features/settings/components/SettingsPage.tsx, src/features/settings/hooks/.gitkeep, src/features/settings/index.ts, src/features/settings/settings.test.tsx</files>
  <action>
Create both feature directory structures:
```
src/features/billing/
  components/
    CheckoutPage.tsx
    BillingSettings.tsx
  hooks/
    .gitkeep
  index.ts
  billing.test.tsx

src/features/settings/
  components/
    SettingsPage.tsx
  hooks/
    .gitkeep
  index.ts
  settings.test.tsx
```

**Billing:**
1. Extract checkout from `_layout.checkout.tsx` into `src/features/billing/components/CheckoutPage.tsx`.
2. Extract billing settings from `_layout.settings.billing.tsx` into `src/features/billing/components/BillingSettings.tsx`.
3. Create barrel export with both components.
4. Move billing-related tests to `src/features/billing/billing.test.tsx`.

**Settings:**
1. Extract the settings component from `_layout.settings.index.tsx` into `src/features/settings/components/SettingsPage.tsx`. This is a big file (~232 lines) with general settings (username, avatar, delete account).
2. Use `@/shared/schemas/username` for USERNAME_MAX_LENGTH (the fix from Plan 01-02).
3. Create barrel export.
4. Move settings-related tests to `src/features/settings/settings.test.tsx`.

For all extractions: move ALL JSX, hooks, helper functions. Use named exports. Update api.* paths to the new domain paths from Plan 01-02.

IMPORTANT: Do NOT modify route files yet -- that happens in Plan 01-04.
  </action>
  <verify>
    <automated>cd /Users/siraj/Desktop/NonDropBoxProjects/feather-starter-convex && npm run typecheck 2>&1 | tail -5 && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>Billing and settings feature folders exist with extracted components, barrel exports, and co-located tests. TypeScript compiles and tests pass.</done>
</task>

</tasks>

<verification>
- `src/features/dashboard/` has components/, hooks/, index.ts, test file
- `src/features/billing/` has components/, hooks/, index.ts, test file
- `src/features/settings/` has components/, hooks/, index.ts, test file
- All barrel exports work
- `npm run typecheck` passes
- `npx vitest run` passes
</verification>

<success_criteria>
- 3 feature folders with extracted components and co-located tests
- Barrel exports for dashboard, billing, settings
- TypeScript and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-modernization/01-03-SUMMARY.md`
</output>
