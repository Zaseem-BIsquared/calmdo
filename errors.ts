/**
 * Re-export from canonical location for backward compatibility.
 * Convex files import via ~/errors (root alias).
 * New code should import from @/shared/errors.
 */
export { ERRORS } from "./src/shared/errors";
