import { query } from "@cvx/_generated/server";
import { auth } from "@cvx/auth";

/**
 * {{pascalCase name}} queries.
 *
 * Customize: Add your query functions here.
 * Each query should check auth if the data is user-specific.
 */
export const list = query({
  args: {},
  handler: async (ctx) => {
    const userId = await auth.getUserId(ctx);
    if (!userId) {
      return [];
    }

    // TODO: Replace with your actual query
    // return await ctx.db.query("{{name}}").collect();
    return [];
  },
});
