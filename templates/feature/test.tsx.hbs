import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import { {{pascalCase name}}Page } from "./components/{{pascalCase name}}Page";
import { renderWithRouter } from "@/test/setup";

describe("{{pascalCase name}}Page", () => {
  it("renders the page heading", async () => {
    await renderWithRouter(<{{pascalCase name}}Page />);
    expect(screen.getByText("{{pascalCase name}}")).toBeInTheDocument();
  });
});
