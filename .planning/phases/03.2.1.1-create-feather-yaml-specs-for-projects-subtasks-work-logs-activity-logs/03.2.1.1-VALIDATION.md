---
phase: 03.2.1.1
slug: create-feather-yaml-specs-for-projects-subtasks-work-logs-activity-logs
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-29
---

# Phase 03.2.1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | N/A — pure YAML authoring phase |
| **Config file** | N/A |
| **Quick run command** | `npm test` (existing tests must still pass) |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Verify YAML syntax with `node -e "require('js-yaml').load(require('fs').readFileSync('path'))"` or visual inspection
- **After every plan wave:** Run `npm test` to ensure no regressions
- **Before `/gsd:verify-work`:** Full suite must be green + no .gen.yaml files remain
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | Status |
|---------|------|------|-------------|-----------|-------------------|--------|
| All tasks | 01-02 | 1-2 | YAML parity | file check | `grep -r '.gen.yaml' src/features/ --include='*.yaml'` returns empty | pending |
| All tasks | 01-02 | 1-2 | Schema match | manual | Compare feather.yaml fields against convex/schema.ts | pending |

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No new test framework needed.

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|----------|------------|-------------------|
| YAML field-to-schema alignment | Semantic comparison, not automatable | For each entity: compare feather.yaml fields against convex/schema.ts table definition |
| Aspirational sections correctness | Domain knowledge required | Verify detailView, derivedData, hooks sections are sensible for each entity |

---

## Validation Sign-Off

- [x] All tasks have verification criteria (YAML syntax + schema match)
- [x] Sampling continuity: every task produces verifiable YAML files
- [x] Wave 0: no new infrastructure needed
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-03-29
